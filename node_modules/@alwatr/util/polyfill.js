import { globalScope } from '@alwatr/logger';
export const win = globalScope;
const requestAnimationFrameFallback = (callback) => setTimeout(() => callback(Date.now()), 1000 / 60);
export const requestAnimationFrame = win.requestAnimationFrame ||
    win.webkitRequestAnimationFrame ||
    win.mozRequestAnimationFrame ||
    requestAnimationFrameFallback;
const requestIdleCallbackFallback = (callback, options) => setTimeout(callback, options?.timeout ?? 2000);
export const requestIdleCallback = win.requestIdleCallback || win.webkitRequestIdleCallback || win.mozRequestIdleCallback || requestIdleCallbackFallback;
//# sourceMappingURL=polyfill.js.map